#include <iostream>
#include <string>
#include <map>
#include<algorithm>
#include<cmath>
#include<queue>
#include<vector>
using namespace std;
using ll = long long;

int main(){
    ll H, W, K, Si, Sj;
    cin >> H >>W >> K;
    cin >> Si >> Sj;
    ll A[H*W+1];
    for(ll i = 1; i <= H*W; i++) cin >> A[i];

    

}