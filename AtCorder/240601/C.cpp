#include <iostream>
#include<cmath>
#include<algorithm>
using namespace std;
using ll = long long;

int main(){
    ll N, M, K;
    ll C[M+1];

    for(ll i = 1; i <= M; i++){
        cin >> C[i];
        ll A[C[i]+1];
        char R;
        for(ll j = 1; j <= C[i]; j++) cin >> A[j];
        cin >> R;

        
    }
}