#include <iostream>
#include <string>
#include <map>
#include<algorithm>
#include<cmath>
#include<queue>
#include<vector>
using namespace std;
using ll = long long;

int main(){
    ll N;
    cin >> N;
    ll A[N], B[N], C[N];
    vector<ll> G[N+1];
    for(ll i = 1; i <= N-1; i++) {
        cin >> A[i] >> B[i] >> C[i];
    }


    return 0;
}