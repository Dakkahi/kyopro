#include <iostream>
#include<cmath>
#include<algorithm>
using namespace std;
using ll = long long;

int main(){
    ll N, K;
    cin >> N >> K;
    ll V[N+1];
    for(int i = 1; i <= N; i++) cin >> V[i];

    //余事象を考える.
    ll cnt = 0;

}